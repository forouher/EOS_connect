# GitHub Pages Configuration for EOS Connect
title: EOS Connect Documentation
description: Intelligent Energy Management & Optimization
url: "https://ohAnd.github.io"
baseurl: "/EOS_connect"

# Theme (we'll use custom HTML/CSS instead of default Jekyll themes)
theme: null

# Exclude from processing
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock

# Defaults
defaults:
  - scope:
      path: ""
    values:
      layout: default

# Markdown processor
markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter: rouge

# Plugins
plugins:
  - jekyll-seo-tag
